
export interface MusicalSection {
  id: string;
  label: string;
  color: string;
  start: number;
  end: number;
}

export interface SectionTemplate {
  label: string;
  color: string;
  tailwind: string;
}
